"use client"

import React from "react";
import BreadCrum from "./BreadCrum";

const UserManagement: React.FC = () => {

  return (
    <div className="">
      <BreadCrum onSearch={() => {}} onOpen={() => {}} />
    </div>
  );
};

export default UserManagement;
